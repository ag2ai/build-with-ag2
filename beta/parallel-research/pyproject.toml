[project]
name = "parallel-research"
version = "0.1.0"
description = "Parallel web research with live fan-out on AG2 Beta"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ag2[gemini,openai,tavily]>=0.12.0",
    "python-dotenv>=1.0.0",
]
