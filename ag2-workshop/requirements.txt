# AG2 Workshop - Dependencies (AG2 Beta API)
# Install: pip install -r requirements.txt
# Python 3.12+ recommended

# Core + all extras the workshop uses, pinned to the verified version:
#   mcp     -> ep23 MCP tools
#   tracing -> ep24 OpenTelemetry spans
#   a2a     -> ep29 A2A protocol deployment
ag2[openai,mcp,tracing,a2a]>=0.13.4
python-dotenv

# Web UI & deployment (ep21 AG-UI, ep22 plain HTML, ep29 deploy)
starlette
uvicorn
httpx

# Voice (ep05 LiveAgent)
sounddevice
numpy

# Testing (ep27)
pytest
pytest-asyncio

# Notebooks
jupyter
ipykernel

# Removed vs the classic workshop:
#   chromadb  -> replaced by KnowledgeConfig + MemoryKnowledgeStore (ep19)
#   streamlit -> replaced by AGUIStream (ep21) and a plain HTML frontend (ep22)
#   manual OpenTelemetry setup -> replaced by the beta TelemetryMiddleware (ep24)
