# Copy this file to `.env` next to run_demo.py and fill in your key.
# `python-dotenv` loads it via `load_dotenv()` at module import.
#
# Gemini accepts either GEMINI_API_KEY or GOOGLE_API_KEY; the underlying
# `google.genai.Client` looks for both.

GEMINI_API_KEY=your-real-gemini-api-key-here
